import 'package:flutter/material.dart';

class ProjectDialog extends StatelessWidget {
  const ProjectDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 150,
      width: 200,
      color: Colors.white,
    );
  }
}
